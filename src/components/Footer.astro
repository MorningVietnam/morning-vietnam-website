---
import { getLangFromPath, navPath, type Lang } from '../i18n/config';
import { useTranslations } from '../i18n/ui';

const year = new Date().getFullYear();
const FORMSPREE_ID = 'YOUR_FORMSPREE_ID'; // TODO: same form ID as NotifyPopup, or create separate "newsletter" form

// Ngôn ngữ hiện tại + hàm dịch (xem src/i18n/ui.ts để sửa chữ)
const lang: Lang = getLangFromPath(Astro.url.pathname);
const t = useTranslations(lang);
// `l()` giữ khách ở đúng ngôn ngữ khi bấm link trong footer.
// Trang chưa dịch → tự trả về bản tiếng Anh, tránh lỗi 404.
const l = (p: string) => navPath(p, lang);
---

<footer class="site-footer2">
  <!-- ZONE 1: Heading + Subscribe + Nav columns -->
  <div class="site-footer2__top">
    <div class="container site-footer2__top-grid">
      <!-- Left: heading + subscribe -->
      <div class="site-footer2__left">
        <h2 class="site-footer2__heading">
          {t('footer.heading.line1')}<br/>{t('footer.heading.line2')}
        </h2>
        <p class="site-footer2__sub">{t('footer.sub')}</p>
        <form
          class="site-footer2__form"
          action={`https://formspree.io/f/${FORMSPREE_ID}`}
          method="POST"
        >
          <input type="hidden" name="_subject" value="Morning Vietnam — Newsletter signup" />
          <label for="footer-email" class="sr-only">{t('footer.emailLabel')}</label>
          <input
            type="email"
            id="footer-email"
            name="email"
            required
            placeholder={t('footer.emailPlaceholder')}
            autocomplete="email"
          />
          <button type="submit" class="site-footer2__submit">{t('footer.subscribe')}</button>
        </form>
        <p class="site-footer2__form-success" hidden>{t('footer.subscribed')}</p>
      </div>

      <!-- Right: 4 nav columns -->
      <div class="site-footer2__nav">
        <div class="site-footer2__col">
          <h3>{t('footer.col.tours')}</h3>
          <ul>
            <li><a href={l('/tours')}>{t('footer.allTours')}</a></li>
            <li><a href={`${l('/tours')}?region=north`}>{t('footer.north')}</a></li>
            <li><a href={`${l('/tours')}?region=soon`}>{t('footer.comingSoon')}</a></li>
          </ul>
        </div>
        <div class="site-footer2__col">
          <h3>{t('footer.col.explore')}</h3>
          <ul>
            <li><a href={l('/explore/about')}>{t('footer.aboutUs')}</a></li>
            <li><a href={l('/explore/vietnam-facts')}>{t('footer.vietnamFacts')}</a></li>
            <li><a href={l('/explore/faq')}>{t('footer.faq')}</a></li>
            <li><a href={l('/explore/travel-guide')}>{t('footer.travelGuide')}</a></li>
          </ul>
        </div>
        <div class="site-footer2__col">
          <h3>{t('footer.col.help')}</h3>
          <ul>
            <li><a href={l('/contact')}>{t('footer.contact')}</a></li>
            <li><a href="mailto:good@morningvietnam.co">{t('footer.emailUs')}</a></li>
            <li><a href="https://wa.me/84961836611" target="_blank" rel="noopener">{t('footer.whatsapp')}</a></li>
            <li><a href={l('/catalogue')}>{t('footer.catalogueB2B')}</a></li>
          </ul>
        </div>
        <div class="site-footer2__col">
          <h3>{t('footer.col.socials')}</h3>
          <ul>
            <li><a href="https://www.instagram.com/morningvietnam.local" target="_blank" rel="noopener noreferrer">Instagram</a></li>
            <li><a href="https://www.facebook.com/morningvietnam.local" target="_blank" rel="noopener noreferrer">Facebook</a></li>
            <li><a href="https://www.tiktok.com/@morningvietnam.local" target="_blank" rel="noopener noreferrer">TikTok</a></li>
          </ul>
        </div>
      </div>
    </div>
  </div>

  <!-- ZONE 2: giant logo image + copyright -->
  <div class="site-footer2__bottom">
    <div class="site-footer2__logo-zone" aria-hidden="true">
      <img
        src="/secondary-lightcyan.png"
        alt=""
        class="site-footer2__logo-img"
        loading="lazy"
      />
    </div>
    <div class="container site-footer2__bottom-row">
      <!-- Số giấy phép là thông tin pháp lý — KHÔNG dịch, KHÔNG đổi -->
      <p class="site-footer2__copy site-footer2__license"><strong>{t('footer.license')}<br/>01-3135/2026/CDLQGVN-GP LHQT</strong></p>
      <div class="site-footer2__copy-legal">
        <p class="site-footer2__copy">© {year} Morning Vietnam Co. Ltd. {t('footer.rights')}</p>
        <div class="site-footer2__legal">
          <a href={l('/privacy-policy')}>{t('footer.privacy')}</a>
          <a href={l('/terms-and-conditions')}>{t('footer.terms')}</a>
        </div>
      </div>
    </div>
  </div>
</footer>

<script>
  // Newsletter form AJAX
  const ff = document.querySelector('.site-footer2__form') as HTMLFormElement | null;
  const fs = document.querySelector('.site-footer2__form-success') as HTMLElement | null;
  ff?.addEventListener('submit', async (e) => {
    e.preventDefault();
    try {
      const res = await fetch(ff.action, {
        method: 'POST',
        body: new FormData(ff),
        headers: { Accept: 'application/json' }
      });
      if (res.ok) {
        ff.style.display = 'none';
        fs?.removeAttribute('hidden');
      }
    } catch {}
  });

  // Logo jump-from-bottom when nav section scrolls into view
  const logoImg = document.querySelector('.site-footer2__logo-img');
  const footerNav = document.querySelector('.site-footer2__nav');
  if (logoImg && footerNav) {
    const obs = new IntersectionObserver(([entry]) => {
      if (entry.isIntersecting) {
        logoImg.classList.add('is-visible');
        obs.disconnect();
      }
    }, { threshold: 0.25 });
    obs.observe(footerNav);
  }
</script>

<style>
  .site-footer2 {
    background: var(--color-java);
    color: var(--color-light-cyan);
    position: relative;
    overflow: hidden;
  }

  /* ── Zone 1 ───────────────────────── */
  .site-footer2__top {
    padding: 96px 0 64px;
  }

  .site-footer2__top-grid {
    display: grid;
    grid-template-columns: 1.2fr 1.8fr;
    gap: 80px;
    align-items: start;
  }

  .site-footer2__heading {
    font-family: var(--font-heading);
    font-size: clamp(28px, 3.5vw, 50px);
    font-weight: 700;
    line-height: 0.95;
    letter-spacing: -0.03em;
    color: var(--color-light-cyan);
    margin-bottom: 24px;
  }

  .site-footer2__sub {
    font-size: 15px;
    line-height: 1.6;
    color: rgba(217, 247, 245, 0.8);
    margin-bottom: 28px;
    max-width: 360px;
  }

  .site-footer2__form {
    display: flex;
    gap: 0;
    max-width: 420px;
    background: rgba(217, 247, 245, 0.14);
    border: 1.5px solid rgba(217, 247, 245, 0.35);
    border-radius: 100px;
    padding: 4px;
  }

  .site-footer2__form input {
    flex: 1;
    background: transparent;
    border: none;
    padding: 12px 20px;
    color: var(--color-light-cyan);
    font-size: 14px;
    font-family: inherit;
    outline: none;
    min-width: 0;
  }

  .site-footer2__form input::placeholder {
    color: rgba(217, 247, 245, 0.55);
  }

  .site-footer2__submit {
    background: var(--color-nordic);
    color: var(--color-light-cyan);
    border: none;
    padding: 12px 28px;
    border-radius: 100px;
    font-family: var(--font-heading);
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.04em;
    cursor: pointer;
    transition: background 0.2s ease;
    white-space: nowrap;
    flex-shrink: 0;
  }

  .site-footer2__submit:hover {
    background: #000;
  }

  .site-footer2__form-success {
    margin-top: 12px;
    color: var(--color-light-cyan);
    font-weight: 600;
    font-size: 14px;
  }

  /* Nav columns */
  .site-footer2__nav {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 32px;
  }

  .site-footer2__col h3 {
    font-family: var(--font-heading);
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--color-light-cyan);
    margin-bottom: 16px;
    opacity: 0.6;
  }

  .site-footer2__col ul {
    display: flex;
    flex-direction: column;
    gap: 10px;
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .site-footer2__col a {
    font-size: 14px;
    font-family: 'Google Sans', var(--font-heading), sans-serif;
    font-weight: 700;
    color: var(--color-light-cyan);
    opacity: 0.85;
    text-decoration: none;
    transition: opacity 0.2s ease, transform 0.2s ease;
    display: inline-block;
  }

  .site-footer2__col a:hover {
    opacity: 1;
    transform: translateX(2px);
  }

  /* ── Zone 2 ───────────────────────── */
  .site-footer2__bottom {
    border-top: 1px solid rgba(217, 247, 245, 0.18);
    overflow: hidden;
  }

  .site-footer2__logo-zone {
    width: 100%;
    padding: 40px 0 0;
    overflow: hidden;
  }

  @keyframes logoJump {
    from { transform: translateY(110%); opacity: 0; }
    to   { transform: translateY(0);    opacity: 1; }
  }

  .site-footer2__logo-img {
    display: block;
    width: 100%;
    height: auto;
    transform: translateY(110%);
    opacity: 0;
    pointer-events: none;
    user-select: none;
  }

  .site-footer2__logo-img.is-visible {
    animation: logoJump 0.75s cubic-bezier(0.22, 1, 0.36, 1) forwards;
  }

  .site-footer2__bottom-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 12px 24px;
    padding: 24px 0 36px;
  }

  .site-footer2__copy {
    font-size: 16px;
    color: rgba(217, 247, 245, 0.65);
  }

  .site-footer2__copy-legal {
    display: flex;
    align-items: center;
    gap: 24px;
    flex-wrap: wrap;
  }

  .site-footer2__legal {
    display: flex;
    gap: 24px;
  }

  .site-footer2__legal a {
    font-size: 16px;
    color: rgba(217, 247, 245, 0.65);
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .site-footer2__legal a:hover {
    color: var(--color-light-cyan);
  }

  /* ── Responsive ───────────────────── */
  @media (max-width: 960px) {
    .site-footer2__top-grid {
      grid-template-columns: 1fr;
      gap: 56px;
    }
    .site-footer2__nav {
      grid-template-columns: repeat(2, 1fr);
      gap: 32px 24px;
    }
  }

  @media (max-width: 560px) {
    .site-footer2__top { padding: 64px 0 48px; }
    .site-footer2__form { max-width: 100%; }
    .site-footer2__logo-zone { padding-top: 24px; }
    .site-footer2__bottom-row {
      flex-direction: column;
      align-items: center;
      gap: 12px;
      text-align: center;
    }
    .site-footer2__copy-legal {
      flex-direction: column;
      align-items: center;
      gap: 8px;
    }
    .site-footer2__legal { justify-content: center; }
    .site-footer2__copy { font-size: 12.8px; }
    .site-footer2__legal a { font-size: 12.8px; }
  }
</style>
